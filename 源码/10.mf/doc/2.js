let scope = {

}

/**
 * 
 * @param {*} name 模块的名称 react
 * version 版本号 "17.0.2"
 */
function register(name,version ) {
  
}
var scope = require.S[name];
var versions = scope[name] = scope[name] || {};

var versions = scope['react']
var activeVersion = versions['17.0.2'];